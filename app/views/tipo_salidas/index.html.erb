<h1>Listing tipo_salidas</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tipo_salidas.each do |tipo_salida| %>
      <tr>
        <td><%= tipo_salida.nombre %></td>
        <td><%= link_to 'Show', tipo_salida %></td>
        <td><%= link_to 'Edit', edit_tipo_salida_path(tipo_salida) %></td>
        <td><%= link_to 'Destroy', tipo_salida, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tipo salida', new_tipo_salida_path %>
